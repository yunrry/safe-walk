# 데이터베이스 설정
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=safewalk

# 사용 예시:
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. 실제 데이터베이스 정보로 값들을 수정
# 3. Python 스크립트 실행: python python/legal_dong_coordinates_to_db.py